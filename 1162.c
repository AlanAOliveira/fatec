#include <stdio.h>
 
int main() {
 
    int N, L, i, j, k;
    
    scanf("%i", &N);    
    int S[N];
    
    for(i=0; i<N; i++){
        scanf("%i", &L);
        int OE[l], OS[l];
        
        for(j=0;j<L;j++){
            scanf(" %i",&OE[j]);
            OS[j] = OE[j];
        }
        
        for(j=0;j<L;j++)
            while()
 
    }return 0;
}
